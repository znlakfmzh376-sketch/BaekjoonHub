-- 코드를 입력하세요
SELECT P.PRODUCT_CODE,SUM(PRICE*SALES_AMOUNT)AS TOTAL_SALE FROM PRODUCT P JOIN OFFLINE_SALE S ON S.PRODUCT_ID=P.PRODUCT_ID GROUP BY P.PRODUCT_CODE ORDER BY TOTAL_SALE DESC, P.PRODUCT_CODE;